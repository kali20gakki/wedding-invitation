# 视觉校验记录

用无头 Chrome（CDP）在三档手机宽度 + 桌面宽度做全页截图，并用读图能力逐张检查。
复现方式：

```bash
# 终端 1：起本地静态服务
python -m http.server 8099 --bind 127.0.0.1
# 终端 2：截图（写入 generated-raw/shots/）
node tools/shoot.mjs 8099
```

## 校验结果

| 视口 | 文档高度 | 横向滚动 | 结论 |
| --- | --- | --- | --- |
| 375×812 | 6400px 级 | 无 | ✅ 通过 |
| 390×844 | 6455px | 无 | ✅ 通过 |
| 430×932 | 6600px 级 | 无 | ✅ 通过 |
| 1280×900 | 6700px 级 | 无 | ✅ 通过（`.phone-shell` 居中的手机版式） |

## 真实邀请信息替换校验（2026-09-19）

已替换为真实信息并复测：

| 位置 | 内容 |
| --- | --- |
| 新人 | 张伟 & 李旭雨 |
| 日期 | 2026年10月2日 · 星期五 |
| 地点 | 云南省罗平县金花玉湖酒店 |
| 日程 | 11:30 午宴 / 14:30 草坪婚礼 / 20:00 After Party |
| 倒计时 | 实时计算，写作时显示 12 天 |
| 住宿默认时间 | 入住 2026-10-01 14:00 / 退房 2026-10-03 12:00 |

同时做了一处结构性调整：**首页姓名改用 HTML 文本而非图片**。
原因：图片会锁死字号，在 390px 宽的 242px 容器里中文姓名会小到不可读，且无法被搜索引擎与读屏识别。
已删除 `assets/wedding-names.svg`，改用 `.hero-cn-name` + `.hero-en-name` 文本样式。

## 交互态校验

| 状态 | 结果 |
| --- | --- |
| 抽卡面板打开 | ✅ 卡背 + 标题正确，背景锁定滚动 |
| 点击翻牌 | ✅ 卡面显示（场景图 + 卡名 + 稀有度 + 祝福语），出现保存/再抽/关闭 |
| `.draw-card` 存在性断言 | ✅ true |

## 过程中发现并修掉的问题

| # | 问题 | 处理 |
| --- | --- | --- |
| 1 | 首页内容溢出一屏，主按钮落在首屏外 | 首页改为 flex 纵向 `space-between` 布局，文案与按钮尺寸收紧，全部落在一屏内 |
| 2 | 纸鹤挤成两列、呈竖排文字 | 改为横向铺开：`--delay` 错开起始相位、`top` 按 53px 递增、`writing-mode: horizontal-tb` 显式固定、`glide` 位移扩到 112vw |
| 3 | 抽卡层标题与内容态不一致（显示卡背却写"吹来了"） | 翻卡改为临时态不落盘；只有点「保存」才写入 `localStorage`；再次打开时标题与提示语与内容保持一致 |
| 4 | `.chime-note` / `.draw-card figcaption b` 缺少样式 | 补上风铃三响祝福条与卡面说明的样式 |
| 5 | 无头截图大部分板块空白 | 非页面缺陷：`.reveal` 的 IntersectionObserver 未触发。截图脚本改为先滚动全页触发入场，再强制可见后截图 |

## 关键行为自动化断言（每次截图自动跑，`home-390` 那一轮）

| 断言项 | 期望 | 实测 |
| --- | --- | --- |
| `#rsvp-form` 是否隐藏 | true（外链模式） | ✅ true |
| `#rsvp-external` 是否显示 | true | ✅ true |
| 外链 href | 飞书问卷链接 | ✅ `https://my.feishu.cn/share/base/form/shrcnFtcbV94GWMm1y3Zh7da99b` |
| 名册状态文案 | `ONLINE` | ✅ ONLINE |
| 地图链接 href | 高德导航链接 | ✅ `https://surl.amap.com/gjQT5wW87AR` |
| 首页姓名文本 | `张伟&李旭雨` | ✅ |
| 倒计时 | 距婚期天数 | ✅ 12 |
| `#crew` 卡片数 | 9 | ✅ 9 |
| 板块编号 | 01–07 连号 | ✅ `01,02,03,04,05,06,07` |

## 未验证项（如实标注）

| 项 | 原因 |
| --- | --- |
| 真机 iOS Safari 的 `navigator.share` / 长按保存 | 无真机环境，需在手机上实测 |
| 微信内打开与分享卡片 | 需部署后实测 |
| `assets/wedding-bgm.mp3` 播放 | 音乐文件尚未提供；缺失时页面已走失败提示分支 |
| 飞书问卷实际提交 | 链接 HTTP 200 已验证可达，但未在真机完成一次真实提交 |
| 抽卡"保存图片"的 fetch 分支 | 截图环境未触发下载/分享面板，仅验证了不抛错 |
| 英文名 / 想对宾客说的话 | 尚未提供，目前英文行用日期占位 |

## 待完成项更新（2026-09-19）

| 状态 | 项目 |
| --- | --- |
| ✅ 完成 | 真实邀请信息（新人 / 日期 / 地点 / 日程 / 倒计时） |
| ✅ 完成 | **飞书问卷链接已接入，外链模式生效**（断言通过） |
| ✅ 完成 | 地图导航链接 |
| ✅ 完成 | 酒店图片转绘 + 嵌入「往那盏灯的路」卡片内、导航按钮上方 |
| ✅ 完成 | 首页姓名改用 HTML 文本（删除 `wedding-names.svg`） |
| ✅ 完成 | `.gitignore` 覆盖 `generated-raw/`、`raw-photos/`、`起风了.jpg` |
| 🟠 待办 | 双人立绘 B10 + `#crew` 第 1 位替换（现为夕阳图占位） |
| 🟡 待办 | 英文名、想对宾客说的话 |
| 🟡 待办 | 背景音乐 `assets/wedding-bgm.mp3` |
| 🟢 可缓 | 抽卡卡面 B13、婚纱照转换 B12 |
| 🟢 可缓 | 部署 GitHub Pages + 回填 `og:image` / `canonical` 真实域名、`admin/` 绝对路径修复 |

## 距婚礼时间（重要）

系统日期 **2026-09-20**，婚期 **2026-10-02**，**仅剩 12 天**。剩余待办按紧急度排序：

1. `RSVP_FORM_URL` 飞书表单链接（不填则宾客无法登记）— 最高优先
2. 地图分享链接（宾客找不到地方）— 最高优先
3. 新人双人立绘 B10 + `#crew` 第 1 位替换（目前用夕阳图占位）
4. 婚纱照风格转换 B12（若时间允许）
5. 背景音乐 `assets/wedding-bgm.mp3`
6. 抽卡卡面 B13（当前用场景图代替，可接受）
7. 部署到 GitHub Pages + 回填 `og:image` / `canonical` 真实域名

---

## 2026-09-20：草坪婚礼时间改 14:30，并修掉同屏查出的两个缺陷

### 改动

| # | 位置 | 改动 |
| --- | --- | --- |
| 1 | `index.html` 04 章行程单 | 草坪婚礼 `14:00` → `14:30` |
| 2 | `index.html` 05 章说明句 | 「11:30 午宴，14:00 草坪婚礼…」→ `14:30` |
| 3 | `style.css` `.schedule-section .restaurant-screen img` | 加 `max-height:clamp(190px,calc(100svh - 636px),400px)` |
| 4 | `style.css` `.story-section` | `contain-intrinsic-size:auto 100svh` → `auto calc(100svh - 168px)` |
| 5 | `style.css` `.coordinate` / `.map-console>p:not(.coordinate)` | `--ink-faint`→`--ink-soft`；`--ink-soft`→`--ink` |
| 6 | `index.html` 04 章行程单 | 追加一行 `20:00` After Party |
| 7 | `index.html` 05 章说明句 | 「…两场都在酒店内」→「11:30 午宴与 14:30 草坪婚礼都在酒店内，20:00 还有 After Party」 |
| 8 | `style.css` `.schedule-section` 间距与场景图 | 两处 24px 间距收到 18px；场景图 `max-height` 改 `clamp(146px,calc(100svh - 698px),400px)` |

### 为什么顺带动了 3~5

三条都是改时间时顺手量出来的既有缺陷，不是这次文案引入的。已用 `git worktree` 拉 `74f82de` 做对照：改前改后 `#schedule` 高度都是 862.48px，文档总高都是 8126px，逐项一致。

**#3 04 章超屏 18px。** 390×844 上这一屏 862px，倒计时卡下沿掉出折线。除场景图外固定约 632px，让图片按剩余空间收即可，和 03 章轮播同一套做法。下限 190px 兜底，避免 360×640 这类矮屏把图压没。

**#4 占位高度算错，滚动条会跳。** `contain-intrinsic-size` 量的是**内容盒**，不含 `.story-section` 上下 64+104 的内边距。写 `100svh` 会让屏幕外章节占位 `100svh+168px`，首屏文档高度凭空多 12%，随着滚动逐屏渲染，滚动条长度一路缩短。改成 `calc(100svh - 168px)` 后文档总高 8126→7836px。

**#5 05 章地图上的字不达标。** 正文压在底图 + 牛皮纸渐隐层上，实测底色亮度 0.59~0.61，`--ink-soft`（0.142）只有 **3.5:1**，11px 小字要求 4.5:1。连纯牛皮纸（0.566）也只到 3.2:1 —— 所以加厚渐隐层救不回来，只能把字压深。改成 `--ink` 后 **7.18:1**。上面那行装饰性 `MEETING POINT` 提到 `--ink-soft`，1.88→3.40。

### 逐屏屏数（真实值，滚进视口后量）

新增 `tools/measure-sections.mjs`。**必须滚进视口再量** —— `.story-section` 开了 `content-visibility:auto`，屏幕外章节量到的是占位值，而不滚动直接量会得出「屏数 1.199」这种假数据，我第一遍就被骗了。

| 视口 | briefing | chime | crew | schedule | arrival | rsvp | draw |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 360×640 | 1.273 | 1.180 | **1.000** | 1.312 | 1.156 | 1.165 | 1.060 |
| 375×667 | 1.222 | 1.147 | **1.000** | 1.259 | 1.109 | 1.118 | 1.017 |
| 390×844 | **1.000** | **1.000** | **1.000** | **1.000** | **1.000** | **1.000** | **1.000** |
| 390×962 | **1.000** | **1.000** | **1.000** | **1.000** | **1.000** | **1.000** | **1.000** |
| 430×932 | **1.000** | **1.000** | **1.000** | **1.000** | **1.000** | **1.000** | **1.000** |

- 390×844 / 390×962 / 430×932：**七屏全部正好一屏**（改前 04 章 1.022）。
- 360×640 / 375×667：**多屏超出，属既有情况**。这两档可用高度只有 478~523px，在字号与图片仍可读的前提下装不下，非本次回归。若必须覆盖 iPhone SE 一档，需要单独做一套矮屏降级。

### 本轮校验

| 项 | 结果 |
| --- | --- |
| `shoot.mjs` 断言 | 全通过（`crewCount: 9`、`days: 12`、`sectionNums 01–07`、外链/地图 href 正确） |
| `overflowX` | 375 / 390 / 430 / 1280 全 false |
| `style.css` 结构 | 花括号 359/359，注释 87/87，无替换字符与乱码 |
| 05 章对比度 | 正文 7.18:1（改前 3.54），标题 7.70，装饰标签 3.40 |
| 04 / 05 章截图 | `read_image` 逐张目视确认：14:30 两处均正确渲染，图注与倒计时卡完整在屏内 |

### 追加：04 章末加 20:00 After Party

行程单从两行变三行，`.menu-board` 高度 252 → 325px（+73），390×844 上这一屏
直接从 844 涨到 914px —— 超 70px。摊法：

- 两处 24px 间距收到 18px，收回 12px；
- 场景图 `max-height` 从 `calc(100svh - 636px)` 改成 `calc(100svh - 698px)`，
  下限 190 → 146px，吃掉剩下 58px。

390×844 上 `calc(100svh - 698px)` 正好等于下限 146px，也就是**图片贴在兜底值上**，
这一档再想省空间没有余量了。代价是场景图从 208px 收到 **146px**（346×146，约 2.4:1 的横带）。
430×932 / 390×962 上不受下限约束，图分别是 234 / 264px，只有 844 高的机型明显变扁。
若更看重图的分量、可以接受 04 章滚动，把 `clamp` 下限调回 210px 即可 ——
这一屏会变成约 904px，需要往下滚 60px。

05 章说明句同步改成「11:30 午宴与 14:30 草坪婚礼都在酒店内，20:00 还有 After Party」——
原文「两场都在酒店内」在三场之后已不成立；After Party 的地点未知，所以没写进"都在酒店内"那半句。

校验：390×844 / 390×962 / 430×932 三档 schedule 仍为 **1.000**；
断言全过（`days: 12`、`crewCount: 9`、`sectionNums 01–07`），`overflowX` 全 false；
04 / 05 两屏 `read_image` 目视确认三行行程与 After Party 均正确渲染。
